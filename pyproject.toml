[build-system]
requires = ["setuptools", "setuptools-scm"]


[project]
name = "fibonacci"
dynamic = ["version"]
description = "A package to calculate the fibonacci sequence"
readme = "README.md"
license = "MIT"

[tool.setuptools_scm]
